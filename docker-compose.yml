services:
  backend:
    build: ./backend
    restart: unless-stopped
    volumes:
      - backend-data:/app
    expose:
      - "5000"

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "9090:80"
    depends_on:
      - backend

volumes:
  backend-data: